<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
        }
        .form-wrap {
            background-color: #fff;
            padding: 15px;
            border-radius: 10px;
            margin-top: 100px;
            box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
        }
        .form-wrap h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        .form-wrap input[type="email"] {
            width: 400px;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 5px;
            border: 1px solid #ddd;
        }
        .form-wrap button[type="submit"] {
            padding: 10px 20px;
            background-color: #007BFF;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-wrap">
            <h1>Password Reset</h1>
            <form method="post" action="/password-reset">
                <input type="email" name="email" placeholder="Enter your email" value="@psmanagement.com" required>
                <button type="submit">Send Reset Link</button>
            </form>
        </div>
    </div>
</body>
</html>